/* One dot look for every component that marks position among siblings with
 * dots (Carousel pagination, StepIndicator). */
export const DOT =
	"h-1.5 w-1.5 rounded-full bg-muted-foreground/35 transition-[width,background-color] duration-(--duration-state)";

export const DOT_ACTIVE = "w-5 bg-primary";
